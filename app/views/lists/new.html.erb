<h1> Ajouter une nouvelle liste de films </h1>

  <%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
  <% end %>


<p> <%= link_to "Home", lists_path  %></p>
